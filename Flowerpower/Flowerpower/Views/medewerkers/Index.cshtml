@model IEnumerable<Flowerpower.Models.medewerkers>

@{
   
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        <a href="@Url.Action("Create")"><button type="button" class="btn btn-success">Nieuw</button></a>
    </p>
    <div class="row">
    <div class="table">
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.medewerkernaam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.medewerkerachternaam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.winkel.winkelnaam)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.medewerkernaam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.medewerkerachternaam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.winkel.winkelnaam)
            </td>
            <td>
                <a href="@Url.Action("Edit", new { id = item.medewerkerid })">
                    <button type="button" class="btn btn-warning">Edit</button>
                </a>

                <a href="@Url.Action("Details", new { id = item.medewerkerid })">
                    <button type="button" class="btn btn-info">Details</button>
                </a>

                <a href="@Url.Action("Delete", new { id = item.medewerkerid })">
                    <button type="button" class="btn btn-danger">Delete</button>
                </a>
            </td>
        </tr>
    }
    
    </table>
        </div>
        </div>
</body>
</html>
